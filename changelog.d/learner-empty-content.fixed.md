- **Locked the "blank lesson body in the Course Player" class of bug with an end-to-end regression guard.** Investigation of the reported empty-content symptom found the delivery pipeline correct at every layer on current `main`: a fresh seed's forked v1.1.0 release (the version `_demo_enrich` publishes and the learner is enrolled in) serves **non-empty content for all 42 items** — verified at the DB layer, through an RLS-enforced query as a NOSUPERUSER role with the org GUC set, and through the real `GET /api/v1/learn/courses/{enrollment_id}` HTTP endpoint (200, 0 blank items). `app/core/fork.py`'s structural sharing correctly carries the parent's content-full `ContentVersion` rows forward for unchanged members (copies `VersionMember.asset_version_id`), appends new content for the changed/added members, and never blanks a body — so the reported symptom does not originate in `fork()`, the `course_structure` query, or the frontend. New guard `tests/fork/test_fork_learner_delivery.py::test_forked_release_delivers_nonempty_content_to_learner` runs the real `fork()` (mirroring `_demo_enrich`: change one `lesson_plan`, add one capstone `lab` + a prerequisite edge), enrolls a learner in the released fork, reads the course through the real endpoint, and asserts **no** `CourseItem.content` is blank — covering structurally-shared, changed, and added members. Proven non-vacuous by fault injection (temporarily emitting empty content in `fork()` turns the guard red on exactly the changed/added lineage keys, then reverted). If the production instance still shows blank bodies, the cause is stale data seeded by an earlier build rather than current code — re-seed via `scripts/seed_prod.py`, or capture the failing enrollment for data forensics.
