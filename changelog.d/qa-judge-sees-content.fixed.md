- **QA judge now reads the actual generated content** — the AI QA reviewer scored a Change Request from its title, rationale, and impact summary only; it never saw the generated lesson/slide/assessment bodies in `change_set`. It now receives those bodies (bounded per-asset and in total, truncation flagged), so `content_accuracy`, `consistency`, and `student_experience` are judged from the content itself — fixing artificially low scores on AI-generated releases.
