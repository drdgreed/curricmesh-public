- **Translation testing T2 — content-layout gate.** Extended the T1 pseudolocalization/overflow harness to prove rendered **course content** (lesson bodies, objectives, assessments, rubrics) lays out under long / CJK / RTL payloads. New `frontend/tests/e2e/content-fixtures.ts` (route-mocked stress content: long unbroken URL/word, Japanese CJK, Arabic RTL, long multi-paragraph markdown), `content-views.ts` (shared view catalog), `content-layout.spec.ts` (overflow gate over the Course Builder item editor + Course Player `LessonContent` × baseline/expand/rtl, plus raw-CJK/RTL baseline cases), and a CI-skipped `content-visual.spec.ts`. Reuses the T1 pseudo engine + overflow detector + `known-offenders.ts` unchanged. **Finding (real, pre-existing):** content renderers lack `overflow-wrap: anywhere`, so a long unbroken token in authored content forces a horizontal page scrollbar across the builder objective rail and the Player's markdown/bullets/rubric — annotated in `known-offenders.ts` for a follow-up app-layout fix (not fixed here). Native CJK/RTL wrap correctly. Docs extended in `docs/TRANSLATION_TESTING.md`.
