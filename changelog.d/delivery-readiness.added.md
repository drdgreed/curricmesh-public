- **Learner delivery readiness pass.** Verified the learner consumption loop end-to-end against the live API (`/health` healthy; login gated on demo-user seed — David ops action documented). Added `backend/scripts/smoke_delivery.py` — a standalone HTTP smoke test (no app import, no DB) that exercises login → catalog → enroll → enrollments → course structure → progress → tutor-ask, printing `[PASS]`/`[FAIL]`/`[GATE]` per step with graceful handling of empty catalog and key-gated 503s. Added `docs/DELIVERY_GOLIVE_CHECKLIST.md` — concrete go-live checklist separating ✅ code-complete items (Player, /learn API, enrollment/progress, tutor Q&A/coach/assess, release→ingest, media transcripts indexed, D5 PII redaction, grounding gate, T3 multilingual) from ⚙️ David's ops actions (provision `EMBEDDING_PROVIDER=hosted` + `EMBEDDING_API_KEY` for live RAG retrieval; provision `TRANSCRIBE_API_KEY` for media indexing; D5 privacy sign-off incl. English-centric redaction caveat; seed demo course + learner via `bootcamp_curriculum.py`) and 🔍 verification steps (smoke script + browser click-through).
